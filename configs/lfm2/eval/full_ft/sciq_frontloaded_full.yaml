model_id: LiquidAI/LFM2-8B-A1B
run_name: lfm2_sciq_frontloaded_full

checkpoint:
  mode: full_adapter
  path: ckpt/lfm2_sciq_frontloaded_full/final_adapter

eval:
  benchmark: sciq
  split: test
  results_dir: eval_results
  run_name: sciq_full_eval

  batch_size: 16
  max_new_tokens: 8
  limit: null

  do_sample: false
  temperature: 0.0

  sort_by_length: true
  compile_model: false
  attn_implementation: sdpa
