model_id: allenai/OLMoE-1B-7B-0924-Instruct
run_name: olmoe_sciq_backloaded_full

checkpoint:
  mode: full_model
  path: ckpt/olmoe_sciq_backloaded_full/final_model

eval:
  benchmark: sciq
  split: test
  results_dir: eval_results
  run_name: sciq_full_eval

  batch_size: 16
  max_new_tokens: 8
  limit: null

  do_sample: false
  temperature: 0.0

  sort_by_length: true
  compile_model: false
  attn_implementation: sdpa